const config=require("./config");const {banner,log,error}=require("./lib/logger");const {startMonitor,stopMonitor}=require("./lib/monitor");const {stopAll}=require("./lib/scheduler");const {connectWhatsApp,shutdown}=require("./connection/whatsapp");let stopping=false;async function start(){config.loadConfig();banner(config);process.on("uncaughtException",e=>error("Uncaught exception:",e));process.on("unhandledRejection",e=>error("Unhandled rejection:",e));const stop=async s=>{if(stopping)return;stopping=true;stopMonitor();stopAll();await shutdown(s)};process.once("SIGINT",()=>stop("SIGINT"));process.once("SIGTERM",()=>stop("SIGTERM"));startMonitor();await connectWhatsApp();log("Core initialized successfully.")}start().catch(e=>{error("Startup failed:",e);process.exitCode=1});
