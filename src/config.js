const fs=require("fs"),path=require("path");const root=path.resolve(__dirname,"..");const file=path.join(root,"config.json");function read(){try{return JSON.parse(fs.readFileSync(file,"utf8"))}catch{return {}}}const c=read();const config={name:String(c.botName||"XMD WhatsApp Bot"),version:"1.0.0",prefix:String(c.prefix||"."),publicMode:c.publicMode!==false,ownerNumbers:Array.isArray(c.ownerNumbers)?c.ownerNumbers.map(String):[],pairingCode:Boolean(c.pairingCode),pairingNumber:String(process.env.PAIRING_NUMBER||c.pairingNumber||""),sessionDir:path.resolve(root,c.sessionDir||"session"),mediaDir:path.resolve(root,c.mediaDir||"media"),databaseFile:path.resolve(root,c.databaseFile||"src/database/data.json"),mediaCleanupMinutes:Number(c.mediaCleanupMinutes||30),logLevel:String(process.env.LOG_LEVEL||c.logLevel||"info")};function validateConfig(){const e=[];if(!config.prefix)e.push("prefix cannot be empty");if(config.pairingCode&&!config.pairingNumber)e.push("pairingNumber is required when pairingCode is enabled");return e}function ensureDirectories(){[config.sessionDir,config.mediaDir,path.dirname(config.databaseFile)].forEach(d=>fs.mkdirSync(d,{recursive:true}))}function loadConfig(){const e=validateConfig();if(e.length)throw Error(e.join("; "));ensureDirectories();return config}module.exports={...config,validateConfig,ensureDirectories,loadConfig};
