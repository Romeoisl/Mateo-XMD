const map=new Map();function on(e,h){if(!map.has(e))map.set(e,new Set());map.get(e).add(h);return()=>off(e,h)}function off(e,h){return map.get(e)?.delete(h)||false}async function emit(e,p){for(const h of [...(map.get(e)||[])])try{await h(p)}catch(err){console.error(`[EVENT:${e}]`,err)}}function stats(){let n=0;for(const s of map.values())n+=s.size;return{events:map.size,listeners:n}}module.exports={on,off,emit,stats};
